{
  "name": "@waslah/api-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@waslah/infrastructure": "workspace:*",
    "@waslah/domain": "workspace:*",
    "@waslah/application": "workspace:*"
  }
}